---
"@opensourcescouting/design-system": patch
---

Fix the `MadeWithBadge` default link target to match the canonical
`OpenSourceScouting` GitHub org casing. Functionally equivalent (GitHub
redirects case-insensitively), corrected for consistency with the repository
metadata.
